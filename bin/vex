#! /usr/bin/env bash

if [ $# != 1 ]; then
    echo "usage: vex {vim-script-file-name}"
    exit 1;
fi

script=$1

# it's better to use 'ex' other than 'vim'
exec ex -S $script -c 'call input("vex done! press enter to quit ...")' -cq
